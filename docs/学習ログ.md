# 学習ログ

日ごとにやったこと・分かったことの記録。新しい日を上に追記する。

---

## 2026-09-23 〜 09-24

### やったこと
- `cmd/pra06` bool 再演習、`pra07` インクリメント/デクリメント、`pra08-assignment` 複数変数の同時宣言・多値を返す関数、
  `pra09-variable-declaration` 変数宣言3形式と `:=` を完了。
- `cmd/` を整理: 完了した練習は `cmd/cleared/` に集約、`praNN-テーマ名` にリネーム。`pra10`〜`pra20` は空の枠。
- 進捗チェックリスト（`docs/進捗チェックリスト.md`）を追加。
- VS Code: Go ファイルの保存時自動整形（`[go] editor.formatOnSave`）を設定。

### 分かったこと

**変数宣言（pra09）**
- 3形式: `var x int`（ゼロ値） / `var a, b, c int`（同じ型をカンマ列挙） / `var ( ... )`（丸括弧でグルーピング）。
- 初期値を付ければ型を省略できる（`var x = 1`）。関数内では `:=` で宣言＋代入を一度に書ける。
- 宣言しただけの変数はゼロ値（数値 `0`、string `""`）。

**コンパイルエラー**
- `=` は既存変数への代入、`:=` は新規宣言。初登場の変数に `=` を使うと `undefined`。
- 宣言した変数を使わないと `declared and not used`。未使用の `import` も `imported and not used`。
- 複数の値を返す関数はシグネチャに戻り値の型を書く（`func fn() (int, int)`）。書かないと `too many return values`。

**日本語入力（IME）の不具合**
- 症状: VS Code で日本語コメントを Enter で確定すると、漢字がひらがなに戻る／直前のスペースが消える。
- 原因: VSCodeVim 拡張（JS 製の Vim 再現）が IME 変換中のキー入力を横取りする。
  Wayland/X11 や環境変数、ibus 削除は無関係だった（回り道）。`--disable-extension vscodevim.vim` で解消を確認。
- 運用: 日本語コメントは Claude に挿入してもらう（CLAUDE.md に明記）。本物の Neovim は変換確定後の文字だけ受け取るため起きない。

**Vim / VS Code のコツ**
- 一括置換: `:%s/println/fmt.Println/g`（範囲選択後なら `:'<,'>s/...`）。
- Redo は `Ctrl+R`（Vim ノーマルモード）。VS Code の Ctrl+Y は Vim 拡張に取られる。
- 整形: `Ctrl+A` → `Ctrl+K` `Ctrl+F`（選択範囲）、`Ctrl+Shift+I`（ファイル全体）。自動保存(`afterDelay`)では整形されない。

### 次回
- struct + メソッド（`reference/02-structs-methods/`）→ `cmd/pra10` に自分で書く。

---

## 2026-09-07

### やったこと
- `cmd/pra01`〜`pra03` を確認（型変換 / bool / string）。基本型フェーズ完了。
- `cmd/pra04` … 独自型 `type myInteger int`、struct の生成4種、`changer()` で型変換いろいろ。
- `cmd/pra05` … Go の全演算子を1つずつ関数にして動作確認。演算子ごとの解説コメントを追加。
- `docs/` に本ファイルを追加。`CLAUDE.md` と `START_HERE.md` を整理。
- `reference/`（Claude 作成のお手本 01〜07）をリポジトリに統合。

### 分かったこと

**struct（次にちゃんとやるテーマ）**
- `type 名 struct { ... }`。フィールド大文字 = 外から見える、小文字 = パッケージ内だけ。
- 生成4種: `T{Field: v}`（推奨）/ `T{v1, v2}`（順番依存・脆い）/ `var x T`（ゼロ値）/ `&T{...}`（ポインタ）。
- メソッドは struct の外に `func (レシーバ) 名前()`。継承なし。
- 値レシーバ = コピー（書き換え不可）、ポインタレシーバ `*T` = 元を書き換え可。型内でどちらかに統一。
- コンストラクタは言語機能でなく慣習の `NewXxx()` 関数。
- 継承の代わり = 埋め込み（`type Dog struct { Animal; Breed string }`）。
- struct は代入で丸ごとコピー（値セマンティクス）。全フィールドが比較可能なら `==` で比較可。

**`go vet` の `string(int)` 警告**
- `string(1234)` は "1234" ではなく **コードポイント U+04D2 = 'Ӓ'**（1文字）。
- `go vet` に抑制コメントは無い。意図が明確な書き方に直すと消える:
  - 文字コードとして扱う → `string(rune(i))`
  - 数字の文字列が欲しい → `strconv.Itoa(i)` / `fmt.Sprintf("%d", i)`

**演算子（pra05 まとめ）**
- 算術: `/` は整数どうしだと切り捨て（`7/2 == 3`）。`%` は余り。`i == (i/j)*j + (i%j)`。
- ビット（2進で1桁ずつ）: `&`=両方1で1 / `|`=どちらか1で1 / `^`=違えば1 / `&^`=右が1の桁を左から消す。
  - 例: `3 &^ 6` → `011` から `110` の桁を消す → `001` = 1。
- シフト: `x << n` = ×2ⁿ、`x >> n` = ÷2ⁿ（切り捨て）。例 `5<<4=80`, `4>>2=1`。

**Go がビット演算子を持つ理由（設計思想）**
- Go の発明ではなく C からの相続。作者（グリーズマー / パイク / トンプソン）は Unix / Plan 9 の人。
- Go の「シンプル」= 機械を隠さない・驚かせない。英語らしく見せることではない。
  `x & mask` は隠れコストゼロなので Go 的にはむしろ理想的にシンプル。関数に包む方が複雑化。
- Go はシステム言語 → ネットワークのフラグ / ファイルフォーマット / 権限(0o755) / 暗号 /
  `iota` + `1<<n` では、ビットが「実装詳細」でなく「扱う対象そのもの」。
- 読みやすさを気にした証拠 = C に無い `&^`（ビットクリア）を Go が追加。
- 日常の Web / CLI コードにはまず出てこない。重要度は `+ - * / %` >>> `& | ^ << >>`。
- 詳細は `go-design-philosophy.md`。

### 次回
- `reference/02-structs-methods/` を読む → `cmd/pra06` に struct の練習を自分で書く。
