# 進捗チェックリスト（cmd/pra01 〜 pra20）

完了したら `[ ]` を `[x]` にする。中身を書いたら自動で終わりではなく、
**動作確認（`go run`）してからチェック**する運用。

最終更新: 2026-09-24

フォルダ名自体を `praNN-テーマ名`（英単語 kebab-case）にリネーム済み。
テーマが決まっていない枠は番号だけ（`pra10` など）のまま。
**クリア（完了）したものは `cmd/cleared/` の中にまとめてある**（`cmd/` 直下は未着手の空枠だけ）。

## 完了（`cmd/cleared/` の中）

- [x] `pra01-type-conversion` — 型変換（int → int64）、関数の戻り値
- [x] `pra02-boolean` — bool、論理演算子 `&&` `||`
- [x] `pra03-string` — string 連結、`strings.Replace`、`len` vs `utf8.RuneCountInString`
- [x] `pra04-custom-type` — 独自型（`type myInteger int`）、struct の生成4種、型変換
- [x] `pra05-operators` — 全演算子（算術 / ビット / シフト）
- [x] `pra06-boolean-review` — bool、論理演算子（pra02 の再演習）
- [x] `pra07-increment-decrement` — インクリメント/デクリメント（`inc++` / `dec--`）
- [x] `pra08-assignment` — 複数変数の同時宣言（`:=`）、複数値を返す関数
- [x] `pra09-variable-declaration` — 変数宣言3形式（`var x int` / カンマ列挙 / `var (...)`）、初期値付き `var`、`:=`

## 未着手（`cmd/` 直下、空の雛形のみ）

- [ ] `pra10` - `(次はここ。テーマ候補: struct + メソッド。`reference/02-structs-methods` 参照)`
- [ ] `pra11` - `(未定)`
- [ ] `pra12` - `(未定)`
- [ ] `pra13` - `(未定)`
- [ ] `pra14` - `(未定)`
- [ ] `pra15` - `(未定)`
- [ ] `pra16` - `(未定)`
- [ ] `pra17` - `(未定)`
- [ ] `pra18` - `(未定)`
- [ ] `pra19` - `(未定)`
- [ ] `pra20` - `(未定)`

内容を書き始めたら `git mv cmd/praNN cmd/praNN-テーマ名` でリネームし、ここも書き換える。

## 使い方

1. `reference/NN-xxx/` を読んで `go run` する
2. 練習課題を対応する `cmd/praNN` に書く
3. `go run ./cmd/praNN` で動作確認
4. ここのチェックを付ける + `CLAUDE.md` の進捗メモ・`学習ログ.md` も更新
